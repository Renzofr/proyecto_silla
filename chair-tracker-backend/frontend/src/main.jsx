import React from "react";
import ReactDOM from "react-dom/client";
import ChairTrackerVital from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ChairTrackerVital />
  </React.StrictMode>
);
